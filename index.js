const currentUser = "Emmanuel";  //defines currentUser

//const welcomeMessage = 'Welcome to Flatbook, ' + (currentUser) + '!';

const welcomeMessage = `Welcome to Flatbook, ${currentUser}!`; //defines welcomeMessage + string concatenation

const excitedWelcomeMessage = welcomeMessage.toUpperCase(); 

const shortGreeting = `Welcome, ${currentUser.slice(0,1)}!` //using slice method to return only first letter of currentUser




